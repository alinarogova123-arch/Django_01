# Пульт охраны банка
Показывает сколько всего пропусков есть с допуском к хранилищу, а так же сколько из них активны.

## Как установить
Вам потребуется адрес и пароль для базы данных, а так же `SECRET_KEY` сайта. Адрес базы данных присвоить переменной окружения `DB_HOST`, пароль присвоить переменной окружения `DB_PASSWORD`,
`SECRET_KEY` сайта присвоить переменной окружения `DB_SECRET_KEY` в файле .env как в примере ниже:

``` bash
DB_HOST=...
DB_PASSWORD=...
DB_SECRET_KEY=...
```

Скрипт запускается командой:
```
python main.py
```

Python3 должен быть уже установлен. Затем используйте pip (или pip3, есть конфликт с Python2) для установки зависимостей:

```
pip install -r requirements.txt
```
Цель проекта
Код написан в образовательных целях на онлайн-курсе для веб-разработчиков dvmn.org.
