# Пульт охраны банка
Показывает сколько всего пропусков есть с допуском к хранилищу, а так же сколько из них активны.

## Как установить

Вам потребуются следующие данные: 

1. `DB_HOST` - Адрес базы данных.
2. `DB_PASSWORD` - Пароль доступа пользователя к базе данных.
3. `SECRET_KEY` - Секретный ключ сайта.
4. `PORT_NUMBER` - Номер сетевого порта.
5. `DB_NAME` - Название базы данных.
6. `USER_NAME` - Имя пользователя для аутентификации.

Все значения присвоить соответвующим переменным окружения в файле .env

Скрипт запускается командой:
```
python main.py
```

Python3 должен быть уже установлен. Затем используйте pip (или pip3, есть конфликт с Python2) для установки зависимостей:

```
pip install -r requirements.txt
```
Цель проекта
Код написан в образовательных целях на онлайн-курсе для веб-разработчиков dvmn.org.
